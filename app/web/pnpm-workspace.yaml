# Supply chain: never resolve a version that has been public for less than a
# week (value in minutes). Most malicious releases are pulled within days.
minimumReleaseAge: 10080

allowBuilds:
  esbuild: true
