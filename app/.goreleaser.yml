project_name: mqtt-washdata
version: 2

before:
  hooks:
    - go mod tidy
    - sh -c "cd web && (command -v pnpm >/dev/null 2>&1 || npm install -g pnpm) && pnpm install --frozen-lockfile && pnpm run build"

builds:
  - id: mqtt-washdata
    main: ./main.go
    binary: mqtt-washdata
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mqtt-home/mqtt-washdata/version.Version={{.Version}} -X github.com/mqtt-home/mqtt-washdata/version.GitCommit={{.Commit}} -X github.com/mqtt-home/mqtt-washdata/version.BuildTime={{.Date}}

dockers:
  - image_templates:
      - "pharndt/mqtt-washdata:{{ .Tag }}-amd64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
    extra_files:
      - web/dist/
    skip_push: false

  - image_templates:
      - "pharndt/mqtt-washdata:{{ .Tag }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
    extra_files:
      - web/dist/
    skip_push: false

docker_manifests:
  - id: tag
    name_template: "pharndt/mqtt-washdata:{{ .Tag }}"
    image_templates:
      - pharndt/mqtt-washdata:{{ .Tag }}-amd64
      - pharndt/mqtt-washdata:{{ .Tag }}-arm64

  - id: latest
    name_template: "pharndt/mqtt-washdata:latest"
    image_templates:
      - pharndt/mqtt-washdata:{{ .Tag }}-amd64
      - pharndt/mqtt-washdata:{{ .Tag }}-arm64

release:
  disable: false
  mode: replace
  draft: false
  prerelease: auto
  make_latest: true
  skip_upload: true
